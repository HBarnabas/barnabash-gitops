---
# !!! ENCRYPT THIS FILE BEFORE COMMITTING !!!
#   sops --encrypt --in-place apps/base/minecraft/secret.sops.yaml
apiVersion: v1
kind: Secret
metadata:
  name: minecraft-rcon
  namespace: minecraft
type: Opaque
stringData:
  # The itzg chart reads the RCON password from the `rcon-password` key.
  rcon-password: CHANGE_ME
