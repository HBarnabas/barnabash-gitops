---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# k3s servicelb hands out the node IP for LoadBalancer Services, so there is no
# IP pool to configure here. Just inherit the shared configs.
resources:
  - ../base
